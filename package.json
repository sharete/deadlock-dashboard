{
  "name": "deadlock-dashboard",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "generate": "node scripts/fetch-dashboard-data.mjs",
    "test": "node tests/run.mjs"
  }
}
